# Only needed when running the dashboard separately from the API — i.e.
# `npm run dev:web` against a deployed Worker.
#
# Leave unset for the normal case: the Worker serves the dashboard and the API
# from one origin, so the client uses relative paths.
# VITE_API_ORIGIN=https://onebook-api.your-subdomain.workers.dev
